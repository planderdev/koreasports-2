--- before/assets/js/data.js
+++ after/assets/js/data.js
@@ -1,3 +1,5 @@
+import {businessContent} from './business-content.js';
+export {businessContent};
 import {aboutContent} from './about-content.js';
 export {aboutContent};
 import {sourcePosts,sourceMedia,sourcePages} from './source-content.js';
@@ -71,9 +73,9 @@
  ci:['대한직장인체육회 · 스포츠피플','기존 사이트 로고','기존 사이트에서 사용하는 로고를 현재 홈페이지에 적용했습니다.'],
  articles:['공정하고 투명한 운영을 위해','정관 및 운영 규정','법적 효력이 없는 검토용 페이지입니다. 공식 정관과 개정 이력은 검토 후 게시합니다.'],
  directions:['체육회에 오시는 길','주소 및 문의 안내','기존 사이트 하단에 공개된 주소와 이메일을 안내합니다.'],
- business:['일상에 스포츠의 가능성을 더합니다','참여 · 교육 · 교류 · 안전','대회와 교육, 동호회 서비스를 통해 나에게 맞는 스포츠를 만나보세요.'],
+ business:[businessContent.business.headline,'',businessContent.business.body],
  reform:['더 열린 체육회를 향한 변화','참여 중심의 서비스 개선','투명한 정보 공개와 쉬운 신청, 접근성 개선을 위한 제안 과제입니다.'],
- operations:['안전하고 즐거운 대회를 만듭니다','대회 운영 안내','공고 확인 → 참가신청 → 접수 확인 → 안전교육 → 대회 참여'],
+ operations:[businessContent.operations.headline,'',businessContent.operations.body],
  talent:['경험이 필요한 곳에, 당신의 역량을','지도자·전문인력 인력풀','관심 분야와 활동 지역을 등록하는 신청입니다.'],
  safety:['즐거움의 시작은, 안전입니다','활동 전 준비부터 현장 대응까지','안전 가이드를 읽고 교육 과정을 통해 기본 수칙을 확인하세요.'],
  culture:['스포츠와 문화가 만나는 순간','문화예술 육성사업','직장인의 문화 참여를 위한 사업 소개 템플릿입니다. 세부 사업은 공식 검토 후 확정합니다.'],
--- before/assets/js/pages/catalog.js
+++ after/assets/js/pages/catalog.js
@@ -1,3 +1,4 @@
+import {businessPage,businessIntro} from './business.js';
 import {aboutPage} from './about.js';
 import {readRichBody} from '../generated/public-body.js';
 import {attachmentMarkup} from '../inquiry-attachments.js';
@@ -9,9 +10,9 @@
 export function listing(type){const q=params();let rows,title,subtitle,render,filters;
  if(type==='events'){rows=r.listEvents(q);title='대회·행사';subtitle='나에게 맞는 스포츠를 찾고, 새로운 도전에 함께하세요.';render=eventCard;filters=select('sport','종목',r.content('sports'),q.sport)+select('region','지역',r.content('regions'),q.region)+select('status','접수 상태',['접수중','접수예정','마감'],q.status);}
  if(type==='clubs'){rows=r.listClubs(q);title=q.featured?'이달의 우수동호회':'Club Matching';subtitle='좋아하는 운동으로 연결되는 우리. 함께할 동료를 만나보세요.';render=clubCard;filters=select('sport','종목',r.content('sports'),q.sport)+select('region','활동 지역',r.content('regions'),q.region)+select('recruiting','모집 상태',[{value:'1',title:'멤버 모집중'}],q.recruiting);}
- if(type==='education'){rows=r.collection('courses').filter(c=>(!q.category||c.category===q.category)&&(!q.q||c.title.includes(q.q)));title='교육·성장';subtitle='더 즐겁고 안전한 스포츠를 위한 배움의 시간.';render=courseCard;filters=select('category','교육 유형',['레슨','안전교육','보수교육'],q.category);}
+ if(type==='education'){rows=r.collection('courses').filter(c=>(!q.category||c.category===q.category)&&(!q.q||c.title.includes(q.q)));title='교육·성장';subtitle='';render=courseCard;filters=select('category','교육 유형',['레슨','안전교육','보수교육'],q.category);}
  if(type==='volunteers'){rows=r.collection('volunteerPrograms').filter(c=>(!q.region||c.region===q.region)&&(!q.q||c.title.includes(q.q)));title='자원봉사';subtitle='스포츠의 가장 가까운 곳에서, 함께 만드는 특별한 하루.';render=c=>`<a class="image-card" href="/volunteer.php?id=${c.id}"><div class="thumb">${image(c)}${badge('모집중')}</div><div class="card-body"><h3>${esc(c.title)}</h3><p class="muted">현장 안내 · 경기 운영 지원 · 안전 지원</p><div class="card-bottom"><span>상세 안내 및 신청</span>${icon('arrow-right-line')}</div></div></a>`;filters=select('region','지역',r.content('regions'),q.region);}
- const p=pageNumber(rows.length);return `${heading(title,subtitle,type==='clubs'?'커뮤니티':'체육회사업')}<div class="container content-section">${type==='clubs'?`<div class="row spread"><p class="muted">새로운 스포츠 메이트를 만나보세요.</p>${button('동호회 등록','/join.php?type=club','secondary small','add-line')}</div>`:''}${filterForm(filters+searchField(q.q))}<div class="result-bar"><span>총 <strong>${rows.length}</strong>건</span>${type==='events'?`<label>정렬 <select data-sort aria-label="대회 정렬"><option value="default">추천순</option><option value="date" ${q.sort==='date'?'selected':''}>개최일순</option></select></label>`:''}</div>${rows.length?`<div class="grid-3">${rows.slice((p-1)*6,p*6).map(render).join('')}</div>`:empty()}${pagination(rows.length,p)}</div>`;
+ const p=pageNumber(rows.length);return `${heading(title,subtitle,type==='clubs'?'커뮤니티':'체육회사업')}<div class="container content-section">${type==='education'?businessIntro('education'):''}${type==='clubs'?`<div class="row spread"><p class="muted">새로운 스포츠 메이트를 만나보세요.</p>${button('동호회 등록','/join.php?type=club','secondary small','add-line')}</div>`:''}${filterForm(filters+searchField(q.q))}<div class="result-bar"><span>총 <strong>${rows.length}</strong>건</span>${type==='events'?`<label>정렬 <select data-sort aria-label="대회 정렬"><option value="default">추천순</option><option value="date" ${q.sort==='date'?'selected':''}>개최일순</option></select></label>`:''}</div>${rows.length?`<div class="grid-3">${rows.slice((p-1)*6,p*6).map(render).join('')}</div>`:empty()}${pagination(rows.length,p)}</div>`;
 }
 export function detail(type){const {id}=params();const key={event:'events',club:'clubs',course:'courses',volunteer:'volunteerPrograms'}[type];const e=type==='event'?r.getEvent(id):r.collection(key).find(x=>x.id===id);if(!e)return notFound();const title={event:'대회·행사',club:'동호회',course:'교육 과정',volunteer:'자원봉사'}[type];const closed=type==='event'&&r.eventStatus(e)!=='접수중'||type==='club'&&!e.recruiting;const pairs=type==='event'?[['개최일',date(e.startsAt)],['접수 기간',`${date(e.registrationStartsAt)} – ${date(e.registrationEndsAt)}`],['장소',e.venue],['참가 대상',e.eligibility],['주관',r.content('associations').find(a=>a.id===e.associationId)?.name+''],['참가 현황',`${e.participantCount} / ${e.capacity}명`]]:type==='club'?[['활동 지역',e.region],['종목',e.sport],['정기 모임',e.meeting],['멤버',`${e.memberCount}명`],['소속 기업',e.organizationId?r.collection('organizations').find(o=>o.id===e.organizationId)?.title:'기업 무소속']]:type==='course'?[['교육 방식',e.format],['교육 분량',e.duration],['수료 조건','3단계 학습 · 필독 확인 · 정답 제출']]:[['기수',e.cohort],['활동 지역',e.region],['연결 대회',r.getEvent(e.eventId)?.title],['희망 역할',e.roles.join(' / ')],['필수 교육','자원봉사자 사전 안전교육']];
  const dl=`<dl class="definition-list">${pairs.map(([k,v])=>`<dt>${k}</dt><dd>${esc(v)}</dd>`).join('')}</dl>`;
@@ -48,7 +49,7 @@
  partnership:{title:'함께 만드는 새로운 가능성',description:'기업·단체의 자원과 스포츠를 연결할 협력 제안을 기다립니다.',label:'기업·단체명',body:'파트너십 제안',hint:'기업·단체 소개, 희망 협력 분야와 제안 내용을 작성해주세요.',submit:'파트너십 문의',category:'기업파트너십',symbol:'shake-hands-line'}
 };
 function inquiryLayout(kind){const c=inquiryTypes[kind];return `<div class="inquiry-layout"><section class="inquiry-guide" aria-labelledby="inquiry-guide-title"><span class="inquiry-symbol">${icon(c.symbol)}</span><h2 id="inquiry-guide-title">${c.title}</h2><p>${c.description}</p><div class="inquiry-contact"><h3>사무국 문의</h3><a href="tel:027837700">${icon('phone-line')}02-783-7700</a><a href="mailto:${esc(r.config().email)}">${icon('mail-line')}${esc(r.config().email)}</a></div></section><form id="inquiry-form" class="inquiry-form" data-category="${c.category}" novalidate><header class="inquiry-form-heading"><h2>${kind==='talent'?'참여 정보':kind==='press-tip'?'제보 작성':'내용 작성'}</h2><span>필수 표시 항목을 입력해주세요.</span></header><div id="inquiry-error" class="error-text" role="alert" tabindex="-1"></div><div class="field compact"><label for="inquiry-title">${c.label} <span class="field-required">필수</span></label><input id="inquiry-title" name="title" required maxlength="80" placeholder="${c.label}을 입력해주세요" aria-describedby="inquiry-title-error"><span class="error-text" id="inquiry-title-error"></span></div><div class="field compact"><label for="inquiry-body">${c.body} <span class="field-required">필수</span></label><p class="inquiry-help" id="inquiry-body-help">${c.hint}</p><textarea id="inquiry-body" name="body" required rows="8" maxlength="1000" placeholder="내용을 입력해주세요" aria-describedby="inquiry-body-help inquiry-body-error inquiry-count"></textarea><div class="inquiry-field-meta"><span class="error-text" id="inquiry-body-error"></span><span id="inquiry-count">0 / 1,000</span></div></div>${['qna','press-tip','partnership'].includes(kind)?attachmentMarkup():''}<div class="inquiry-confirm"><label class="check-label"><input id="inquiry-confirm" name="confirmed" type="checkbox" required aria-describedby="inquiry-confirm-error">작성한 내용을 확인했습니다. <span class="field-required">필수</span></label><span class="error-text" id="inquiry-confirm-error"></span></div><footer class="inquiry-actions"><p>작성한 내용을 확인한 후 접수해주세요.</p><button class="button large" type="submit">${c.submit}</button></footer></form></div>`;}
-export function contentPage(){const id=params().id,p=r.content('pageContents')[id];if(!p)return notFound();if(['vision','vision-1','vision-2','purpose'].includes(id))return aboutPage(id,p);if(p.sourceUrl)return sourcePage(p);const parent=r.content('navigation').find(n=>n.items.some(x=>x.id===id||x.children?.some(child=>child.id===id)));let special='';if(id==='faq')special=`<div class="accordion">${r.content('faqs').map(f=>`<details><summary>${f.q}</summary><p>${f.a}</p></details>`).join('')}</div>`;
+export function contentPage(){const id=params().id,p=r.content('pageContents')[id];if(!p)return notFound();if(['vision','vision-1','vision-2','purpose'].includes(id))return aboutPage(id,p);if(['business','operations'].includes(id))return businessPage(id,p);if(p.sourceUrl)return sourcePage(p);const parent=r.content('navigation').find(n=>n.items.some(x=>x.id===id||x.children?.some(child=>child.id===id)));let special='';if(id==='faq')special=`<div class="accordion">${r.content('faqs').map(f=>`<details><summary>${f.q}</summary><p>${f.a}</p></details>`).join('')}</div>`;
  else if(['qna','safety-proposal','talent','press-tip'].includes(id))return `${heading(p.title,'',parent?.title||'체육회 안내')}<div class="container content-section">${inquiryLayout(id)}</div>`;
  else if(id==='members-only')special=r.getCurrentMember()?`<div class="panel"><h3>회원 전용 자료</h3>${button('자료실 바로가기','/board.php?category=자료실','secondary')}</div>`:empty('로그인이 필요합니다','회원으로 로그인하면 회원 전용 자료를 볼 수 있습니다.','/login.php');
  else if(id==='associations')special=`<div class="grid-3">${r.content('associations').map(a=>`<div class="panel"><h3>${a.name}</h3><p>공통 회원, 대회와 교육을 연결합니다.</p>${button('종목 대회 보기',`/events.php?sport=${a.sport}`,'secondary small')}</div>`).join('')}</div>`;
--- before/assets/js/pages/member.js
+++ after/assets/js/pages/member.js
@@ -1,3 +1,4 @@
+import {businessIntro} from './business.js';
 import {bindAttachments,storeAttachments,downloadAttachment} from '../inquiry-attachments.js';
 import * as r from '../services/repository.js';
 import {esc,icon,badge,button,empty,heading,notFound,date,field,select,modal,toast,requireMember,more} from '../renderers.js';
@@ -29,7 +30,7 @@
  return `${heading('마이페이지',`${m.name}님, 오늘도 즐거운 스포츠 생활을 응원합니다.`)}<div class="container content-section"><nav class="tabs underline dashboard-tabs">${tabs.map(([id,label])=>`<a href="?tab=${id}" class="${tab===id?'active':''}">${label}</a>`).join('')}</nav>${body}</div>`;
 }
 export function qualification(){const tab=params().tab||'programs',m=r.getCurrentMember();let body;if(tab==='programs')body=`<div class="grid-3">${r.collection('qualificationPrograms').map(p=>`<div class="course-card"><div class="course-icon">${icon('medal-line')}</div>${badge('검증')}<h3>${esc(p.title)}</h3><p>${esc(p.summary)}</p><div class="form-actions">${button('자격검증 신청',`/apply.php?kind=qualification&id=${p.id}`,'small')}</div></div>`).join('')}</div>`;else if(!m)body=empty('로그인이 필요합니다','회원으로 로그인하면 합격 및 발급 내역을 확인할 수 있습니다.','/login.php');else{const certs=r.getMemberDashboard(m.id).certificates;body=`${certs.length?`<div class="stack">${certs.map(c=>`<div class="panel row spread"><div><h3>${esc(c.title)}</h3><p>${c.number} · ${c.status}</p></div><button class="button" data-issue="${c.id}" data-reissue="${tab==='reissue'}">${tab==='reissue'?'재발급 신청':c.issuedAt?'자격증 보기':'자격증 발급'}</button></div>`).join('')}</div>`:empty('합격 내역이 없습니다','자격검증 신청 후 관리자에서 결과를 처리할 수 있습니다.')}`;}
- return `${heading('자격검증','스포츠의 전문성을 확인하고, 다음 단계로 성장하세요.')}<div class="container content-section"><nav class="tabs underline">${[['programs','자격검증신청'],['results','합격조회·발급'],['reissue','재발급신청']].map(([id,t])=>`<a href="?tab=${id}" class="${tab===id?'active':''}">${t}</a>`).join('')}</nav>${body}</div>`;
+ return `${heading('자격검증')}<div class="container content-section">${businessIntro('qualification')}<nav class="tabs underline">${[['programs','자격검증신청'],['results','합격조회·발급'],['reissue','재발급신청']].map(([id,t])=>`<a href="?tab=${id}" class="${tab===id?'active':''}">${t}</a>`).join('')}</nav>${body}</div>`;
 }
 export function learn(){const id=params().id,c=r.collection('courses').find(x=>x.id===id);if(!c)return notFound();if(!r.getCurrentMember())return requireMember();const d=r.getMemberDashboard(r.getCurrentMember().id);if(!d.applications.some(a=>a.kind==='course'&&a.targetId===id&&a.status!=='취소'))return `${heading(c.title)}<div class="container content-section">${empty('교육 신청 후 학습할 수 있습니다','교육 상세에서 신청을 완료해주세요.',`/course.php?id=${id}`)}</div>`;const p=d.progress.find(x=>x.courseId===id);return `${heading(c.title,'학습단계 열람, 필독 확인 및 정답 제출 후 이수가 완료됩니다.')}<div class="container content-section narrow"><div class="result-bar"><strong id="progress-label">${p?.status||'미시작'} · ${p?.steps.length||0}/3단계</strong></div><div class="learning-steps">${r.content('lessons').map((l,i)=>`<button data-lesson="${i}">${l.title} ${p?.steps.includes(i)?'✓':''}</button>`).join('')}</div><div class="panel learning-body" id="lesson-content"><h2 style="font-size:var(--type-title-3-size);line-height:var(--type-title-3-line);letter-spacing:var(--type-title-3-tracking)">학습을 시작해주세요</h2><p>위의 학습단계를 선택해 자료를 읽어주세요. 페이지를 여는 것만으로 수료 처리되지 않습니다.</p></div>${['course-1','course-5'].includes(id)?`<div class="panel" style="margin-top:var(--space-24)"><h3>필수 현장 관찰 영상</h3><video id="learning-video" controls playsinline preload="metadata" style="width:100%;border-radius:var(--radius-8);margin-top:var(--space-20)" poster="/assets/images/video-poster.jpg" src="/assets/videos/field-training.mp4" aria-label="축구 현장 관찰 영상"></video><p id="video-status" class="learning-help">${p?.videoWatched?'시청 완료':'학습단계를 먼저 열고 영상을 끝까지 시청해주세요.'}</p></div>`:''}<form id="complete-course" data-id="${id}" class="panel" style="margin-top:var(--space-24)"><h3>안전 이해도 확인</h3><p>활동 중 이상 징후가 나타나면 어떻게 해야 할까요?</p><label class="check-label"><input type="radio" name="answer" value="continue" required> 계속 운동하며 경과를 봅니다.</label><label class="check-label"><input type="radio" name="answer" value="stop"> 즉시 멈추고 담당자에게 알립니다.</label><hr><a class="more" href="/assets/safety-guide.txt" target="_blank">${icon('file-text-line')}안전 가이드 자료 열기 (새 창)</a><label class="check-label"><input type="checkbox" name="read"> 안전 가이드와 학습 내용을 모두 읽었습니다.</label><div class="error-text" id="learning-error" role="alert"></div><div class="form-actions"><button class="button" type="submit">이수 확인 ${icon('check-line')}</button></div></form></div>`;}
 function validate(form){let ok=true;form.querySelectorAll('[required]').forEach(input=>{const valid=input.checkValidity();input.setAttribute('aria-invalid',String(!valid));const error=document.getElementById(`${input.id}-error`);if(error)error.textContent=valid?'':input.type==='email'?'이메일 형식을 확인해주세요.':'필수 항목을 입력해주세요.';if(!valid)ok=false;});if(!ok){form.querySelector('[aria-invalid=true]')?.focus();}return ok;}
--- before/assets/css/pages.css
+++ after/assets/css/pages.css
@@ -1064,6 +1064,41 @@
 .history-year ul { list-style: none; padding: 0; margin: 0; }
 .history-year li { display: grid; grid-template-columns: 64px minmax(0,1fr); gap: var(--space-16); padding: var(--space-12) 0; }
 .history-year time { color: var(--semantic-label-alternative); }
+/* Business introduction and event experience; no shared control overrides. */
+.business-intro { max-width: 880px; margin-bottom: var(--space-48); }
+.business-intro h2 { margin: var(--space-16) 0 var(--space-24); font-size: var(--type-title-1-size); line-height: var(--type-title-1-line); letter-spacing: var(--type-title-1-tracking); word-break: keep-all; overflow-wrap: anywhere; }
+.business-intro p,.business-page p { font-size: var(--type-body-1-reading-size); line-height: var(--type-body-1-reading-line); letter-spacing: var(--type-body-1-reading-tracking); color: var(--semantic-label-alternative); word-break: keep-all; overflow-wrap: anywhere; }
+.business-page > section + section { margin-top: var(--space-64); }
+.business-page > section:not(.business-intro) > h2,.business-page .business-guidance h2 { font-size: var(--type-title-2-size); line-height: var(--type-title-2-line); letter-spacing: var(--type-title-2-tracking); margin-bottom: var(--space-24); word-break: keep-all; overflow-wrap: anywhere; }
+.business-page h3 { font-size: var(--type-title-3-size); line-height: var(--type-title-3-line); letter-spacing: var(--type-title-3-tracking); word-break: keep-all; overflow-wrap: anywhere; }
+.business-page h4 { font-size: var(--type-headline-1-size); line-height: var(--type-headline-1-line); letter-spacing: var(--type-headline-1-tracking); margin-bottom: var(--space-16); }
+.business-page .business-field { display: grid; grid-template-columns: 1fr 2fr; gap: var(--space-48); padding: var(--space-40) 0; border-top: var(--border-thin) solid var(--semantic-line-solid-normal); }
+.business-page .business-number { display: block; color: var(--semantic-primary-normal); font-size: var(--type-label-1-size); line-height: var(--type-label-1-line); letter-spacing: var(--type-label-1-tracking); font-weight: 600; margin-bottom: var(--space-16); }
+.business-page .business-points { display: flex; flex-wrap: wrap; gap: var(--space-12) var(--space-28); margin: var(--space-24) 0 0; padding-left: var(--space-20); font-size: var(--type-label-1-size); line-height: var(--type-label-1-line); }
+.business-page .business-points li::marker { color: var(--semantic-primary-normal); }
+.business-page .business-links { display: flex; flex-wrap: wrap; align-items: center; gap: var(--space-12); margin-top: var(--space-28); }
+.business-page .business-columns { display: grid; grid-template-columns: repeat(3,minmax(0,1fr)); gap: var(--space-32); margin-top: var(--space-32); }
+.business-page .business-columns article { border-top: var(--border-thin) solid var(--semantic-line-solid-normal); padding-top: var(--space-24); }
+.business-page .business-columns p { margin-top: var(--space-16); }
+.business-page .business-photo-grid { display: grid; grid-template-columns: repeat(2,minmax(0,1fr)); gap: var(--space-40) var(--space-32); margin-top: var(--space-32); }
+.business-page figure { margin: 0; min-width: 0; }
+.business-page .business-photo-frame { display: grid; place-items: center; height: 420px; padding: var(--space-20); background: var(--semantic-background-normal-alternative); border-radius: var(--radius-16); }
+.business-page .business-photo-frame img { display: block; max-width: 100%; max-height: 380px; width: auto; height: auto; }
+.business-page figcaption { margin-top: var(--space-24); }
+.business-page figcaption p { margin-top: var(--space-8); }
+.business-page .business-guidance { padding: var(--space-40); border-radius: var(--radius-16); background: var(--semantic-background-normal-alternative); }
+@media (max-width: 800px) {
+ .business-page .business-field { grid-template-columns: 1fr; gap: var(--space-24); }
+ .business-page .business-columns { grid-template-columns: 1fr; }
+}
+@media (max-width: 640px) {
+ .business-intro h2 { font-size: var(--type-title-2-size); line-height: var(--type-title-2-line); letter-spacing: var(--type-title-2-tracking); }
+ .business-page > section + section { margin-top: var(--space-48); }
+ .business-page .business-photo-grid { grid-template-columns: 1fr; }
+ .business-page .business-photo-frame { height: auto; }
+ .business-page .business-guidance { padding: var(--space-24); }
+}
+
 /* Vision and purpose: scoped editorial content, sharing the public type scale. */
 .about-page .about-intro { max-width: 880px; }
 .about-page h2 { font-size: var(--type-title-2-size); line-height: var(--type-title-2-line); letter-spacing: var(--type-title-2-tracking); font-weight: 700; word-break: keep-all; overflow-wrap: anywhere; }
--- before/assets/js/business-content.js
+++ after/assets/js/business-content.js
@@ -0,0 +1,171 @@
+// Approved copy from business-pages-codex-instructions.md.
+export const businessContent = {
+  "business": {
+    "headline": "대회와 교육, 교류로 넓히는 직장인 스포츠",
+    "body": "대한직장인체육회는 국내 스포츠대회와 국제교류, 교육과 전문인력 양성, 홍보·후원 협력을 통해 직장인 스포츠의 참여 기반을 넓혀갑니다. 직장인과 지역, 기업과 스포츠단체를 연결하며 함께 참여할 기회를 만들어갑니다.",
+    "label": "OUR BUSINESS",
+    "fields": [
+      {
+        "title": "직장인 스포츠대회",
+        "subtitle": "다양한 종목에서 함께 뛰는 기회",
+        "body": "축구·농구·마라톤·골프·볼링 등 다양한 종목의 대회와 행사를 통해 직장인들이 운동하고 교류할 수 있는 장을 마련합니다. 전국 규모의 대회부터 지역과 연계한 행사까지, 직장인과 동호인의 생활체육 참여를 넓혀갑니다.",
+        "items": [
+          "전국 직장인·동호인 대회",
+          "지역 연계 스포츠 행사",
+          "종목별 대회와 페스티벌"
+        ],
+        "links": [
+          {
+            "label": "대회운영 알아보기",
+            "menu": "대회운영"
+          }
+        ]
+      },
+      {
+        "title": "국제 스포츠교류",
+        "subtitle": "스포츠로 만나는 더 넓은 세계",
+        "body": "CSIT 네트워크와 World Sports Games(WSG) 참가 경험을 바탕으로 국제 스포츠교류를 이어갑니다. 해외 스포츠단체와의 만남과 국제행사 참여를 통해 협력 관계를 넓히고, 국제대회 유치를 위한 기반을 다져갑니다.",
+        "items": [
+          "CSIT 네트워크 교류",
+          "WSG 참가와 국제행사 참여",
+          "국제대회 유치를 위한 협력"
+        ]
+      },
+      {
+        "title": "스포츠 교육·자격",
+        "subtitle": "현장을 뒷받침하는 전문성",
+        "body": "지도자·심판 양성과 종목별 교육, 자격 검정 등을 통해 스포츠 현장을 뒷받침하는 전문 역량을 높이고자 합니다. 직장인 스포츠의 참여 기반과 함께 이를 지원하는 인적 기반을 넓혀갑니다.",
+        "items": [
+          "지도자·심판 양성",
+          "종목별 교육",
+          "자격 검정"
+        ],
+        "links": [
+          {
+            "label": "교육사업 알아보기",
+            "menu": "교육사업"
+          },
+          {
+            "label": "자격검증 안내",
+            "menu": "자격검증"
+          }
+        ]
+      },
+      {
+        "title": "스포츠 홍보·협력",
+        "subtitle": "활동을 알리고, 협력의 기회를 넓힙니다",
+        "body": "스포츠피플과 디지털 채널을 통해 직장인 스포츠의 현장과 활동을 알립니다. 기업·기관과의 미디어·스폰서십 협력을 통해 스포츠를 매개로 함께할 기회를 넓혀갑니다.",
+        "items": [
+          "스포츠 활동과 현장 홍보",
+          "디지털 채널을 통한 정보 전달",
+          "기업·기관과의 후원 협력"
+        ],
+        "links": [
+          {
+            "label": "기업파트너십 알아보기",
+            "menu": "기업파트너십"
+          }
+        ]
+      }
+    ],
+    "cooperation": {
+      "title": "지역과 기업, 국제 네트워크가 함께합니다",
+      "body": "지방자치단체와 기업, 국제 스포츠단체와의 협력을 통해 직장인 스포츠가 이어질 수 있는 기반을 넓혀갑니다.",
+      "items": [
+        {
+          "title": "지방자치단체",
+          "body": "지역의 생활체육과 스포츠관광을 연결하는 대회·행사 협력"
+        },
+        {
+          "title": "기업·기관",
+          "body": "직장인 스포츠 복지와 조직문화, 홍보·후원을 연계하는 협력"
+        },
+        {
+          "title": "국제 스포츠단체",
+          "body": "국제교류와 대회 참가, 유치 활동을 위한 네트워크 협력"
+        }
+      ]
+    }
+  },
+  "operations": {
+    "headline": "함께 뛰는 현장, 함께 나누는 스포츠",
+    "body": "대한직장인체육회는 다양한 종목의 대회와 행사를 통해 직장인과 동호인이 만나고 교류하는 현장을 만들어왔습니다. 전국 규모의 대회와 지역 연계 행사, 종목별 대회를 통해 생활체육 참여의 기회를 넓혀갑니다.",
+    "label": "SPORTS EVENTS",
+    "fields": [
+      {
+        "title": "전국 직장인 스포츠대회",
+        "body": "전국의 직장인과 동호인이 함께하는 대회를 통해 지역과 일터를 넘어 스포츠로 교류하는 기회를 마련합니다."
+      },
+      {
+        "title": "지역 연계 대회·행사",
+        "body": "지역과 연계한 직장인·동호인 대축전, 자선 골프대회, 종목별 페스티벌 등 다양한 스포츠 행사를 개최해왔습니다."
+      },
+      {
+        "title": "종목별 대회",
+        "body": "축구·농구·풋살·골프·볼링·테니스·배드민턴·댄스스포츠 등 여러 종목에서 대회 개최 경험을 쌓아왔습니다."
+      }
+    ],
+    "galleryTitle": "사진으로 만나는 대회 현장",
+    "galleryIntro": "직장인과 동호인이 함께한 대회의 순간을 소개합니다.",
+    "photos": [
+      {
+        "title": "전국 직장인 축구대회",
+        "body": "축구를 통해 직장인과 동호인이 함께한 대회 현장",
+        "src": "/assets/images/business/football.jpeg",
+        "width": 531,
+        "height": 552,
+        "alt": "전국 직장인 축구대회 현수막 앞 단체 사진"
+      },
+      {
+        "title": "K직장인농구리그",
+        "body": "K직장인농구리그에서 함께한 참가자들의 모습",
+        "src": "/assets/images/business/basketball.jpeg",
+        "width": 405,
+        "height": 255,
+        "alt": "K직장인농구리그 참가자 단체 사진"
+      },
+      {
+        "title": "대한직장인체육회 풋살대회",
+        "body": "팀으로 함께 뛰며 스포츠의 즐거움을 나누는 풋살 현장",
+        "src": "/assets/images/about/domestic-sports.jpeg",
+        "width": 600,
+        "height": 400,
+        "alt": "대한직장인체육회 풋살대회 경기 모습"
+      },
+      {
+        "title": "배드민턴 대회",
+        "body": "참가자들이 경기를 통해 실력을 겨루고 교류하는 배드민턴 현장",
+        "src": "/assets/images/business/badminton.jpeg",
+        "width": 600,
+        "height": 400,
+        "alt": "배드민턴 대회에서 경기하는 선수들"
+      }
+    ],
+    "guidance": {
+      "title": "대회별 일정과 참가 안내를 확인하세요",
+      "body": "대회마다 일정과 참가 대상, 신청 방법이 다를 수 있습니다. 대회·행사 목록과 공고에서 상세 내용을 확인해 주세요.",
+      "links": [
+        {
+          "label": "대회·행사 보기",
+          "menu": "대회·행사"
+        },
+        {
+          "label": "대회 공고 보기",
+          "menu": "대회소식/공지"
+        },
+        {
+          "label": "현장갤러리 보기",
+          "menu": "현장갤러리"
+        }
+      ]
+    }
+  },
+  "education": {
+    "headline": "스포츠 현장을 함께 만드는 배움",
+    "body": "대한직장인체육회는 지도자·심판 양성과 종목별 교육을 통해 직장인 스포츠를 뒷받침하는 전문성을 높이고자 합니다. 교육과정별 대상과 내용을 확인하고, 관심 분야에 맞는 배움의 기회를 살펴보세요."
+  },
+  "qualification": {
+    "headline": "종목별 전문성을 위한 자격 검정",
+    "body": "대한직장인체육회는 종목별 자격 검정을 교육·인증 사업의 한 영역으로 소개하고 있습니다. 자격별 응시 대상과 검정 기준, 신청 절차를 확인해 주세요."
+  }
+};
--- before/assets/js/pages/business.js
+++ after/assets/js/pages/business.js
@@ -0,0 +1,28 @@
+import * as r from '../services/repository.js';
+import {esc, heading} from '../renderers.js';
+
+function links(items = []) {
+  const menus = r.content('navigation').flatMap(group => group.items.flatMap(item => [item, ...(item.children || [])]));
+  return items.map((item, index) => {
+    const target = menus.find(menu => menu.title === item.menu);
+    return target ? `<a class="button ${index ? 'ds-text small' : 'secondary'}" href="${esc(target.url)}">${esc(item.label)}</a>` : '';
+  }).join('');
+}
+
+export function businessIntro(key) {
+  const item = r.content('businessContent')[key];
+  return `<section class="business-intro" aria-labelledby="business-intro-title">${item.label ? `<span class="eyebrow">${esc(item.label)}</span>` : ''}<h2 id="business-intro-title">${esc(item.headline)}</h2><p>${esc(item.body)}</p></section>`;
+}
+
+export function businessPage(id, page) {
+  const data = r.content('businessContent')[id];
+  const number = index => `<span class="business-number" aria-hidden="true">0${index + 1}</span>`;
+  let content;
+  if (id === 'business') {
+    const cooperation = data.cooperation;
+    content = `<section class="business-fields" aria-labelledby="business-fields-title"><h2 id="business-fields-title">주요 사업 분야</h2>${data.fields.map((item, index) => `<article class="business-field"><header>${number(index)}<h3>${esc(item.title)}</h3></header><div><h4>${esc(item.subtitle)}</h4><p>${esc(item.body)}</p><ul class="business-points">${item.items.map(text => `<li>${esc(text)}</li>`).join('')}</ul>${item.links ? `<div class="business-links">${links(item.links)}</div>` : ''}</div></article>`).join('')}</section><section class="business-cooperation"><h2>${esc(cooperation.title)}</h2><p>${esc(cooperation.body)}</p><div class="business-columns">${cooperation.items.map(item => `<article><h3>${esc(item.title)}</h3><p>${esc(item.body)}</p></article>`).join('')}</div></section>`;
+  } else {
+    content = `<section class="business-fields" aria-labelledby="business-fields-title"><h2 id="business-fields-title">주요 개최 분야</h2><div class="business-columns">${data.fields.map((item, index) => `<article>${number(index)}<h3>${esc(item.title)}</h3><p>${esc(item.body)}</p></article>`).join('')}</div></section><section class="business-gallery"><h2>${esc(data.galleryTitle)}</h2><p>${esc(data.galleryIntro)}</p><div class="business-photo-grid">${data.photos.map(item => `<figure><div class="business-photo-frame"><img src="${esc(item.src)}" alt="${esc(item.alt)}" width="${item.width}" height="${item.height}" loading="lazy" decoding="async"></div><figcaption><h3>${esc(item.title)}</h3><p>${esc(item.body)}</p></figcaption></figure>`).join('')}</div></section><section class="business-guidance"><div><h2>${esc(data.guidance.title)}</h2><p>${esc(data.guidance.body)}</p></div><div class="business-links">${links(data.guidance.links)}</div></section>`;
+  }
+  return `${heading(page.title, '', '체육회사업')}<article class="container content-section business-page">${businessIntro(id)}${content}</article>`;
+}

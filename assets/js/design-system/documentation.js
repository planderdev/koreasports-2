import {variants, implementationCode} from './specimens.js';
import {esc,icon,toast} from '../renderers.js';
import {allDocs,foundations,components,utilities} from './catalog.js';
import {tokens,tokenTable,renderFoundation} from './foundations.js';
import {renderComponent,renderUtility,bindExamples} from './components.js';
const url=(section,item='overview')=>`/design-system.php?section=${section}&item=${item}`;
const sectionNames={foundations:'Foundations',components:'Components',utilities:'Utilities'};
function current(){const p=new URLSearchParams(location.search),section=Object.hasOwn(sectionNames,p.get('section'))?p.get('section'):'foundations';return {section,item:p.get('item')||'overview'};}
const link=d=>`<a href="${url(d.section,d.id)}"><strong>${d.name}</strong><span>${esc(d.description)}</span>${icon('arrow-right-up-line')}</a>`;
function overview(section){const list=section==='components'?components:utilities,groups=[...new Set(list.map(d=>d.group))];return `<div class="ds-doc-lead"><span class="ds-eyebrow">KWSA DESIGN SYSTEM / 2.0</span><h1>${sectionNames[section]}</h1><p>${section==='components'?`${components.length}개 UI 요소. 구조와 상태, 실제 동작, 마크업을 함께 확인합니다.`:`${utilities.length}개 웹 보조 도구. 레이아웃·포커스·레이어·테마·서식 처리의 대응 구현을 확인합니다.`}</p></div><div class="ds-section-index">${groups.map(g=>`<a href="#group-${g.replaceAll(' ','-')}">${g}<span>${list.filter(d=>d.group===g).length}</span></a>`).join('')}</div>${section==='utilities'?'<div class="ds-callout">PHP + Vanilla JavaScript 구현입니다. 이름은 Montage 문서의 분류를 따르되 React hook과 컴포넌트 API를 그대로 제공하지 않습니다. 상세 페이지에 대응 방식과 한계를 명시합니다. iOS·Android 전용 항목은 이 웹 프로젝트의 범위에서 제외합니다.</div>':''}${groups.map(g=>`<section class="ds-doc-section" id="group-${g.replaceAll(' ','-')}"><h2>${g}</h2><div class="ds-catalog-grid">${list.filter(d=>d.group===g).map(link).join('')}</div></section>`).join('')}`;}
const anatomy={Actions:['Container','Leading icon','Label','Trailing icon'],Contents:['Container','Visual','Primary content','Supporting content'],Feedback:['Status symbol','Title / Message','Optional action'],Loading:['Indicator','Accessible status'],Navigations:['Navigation container','Current item','Destination / Panel'],Presentation:['Trigger','Floating surface','Content','Dismiss action'],'Selection and input':['Label','Control','Value','Helper / Error']};
function apiRows(d){const id=d.id;
 if(['button','icon-button','text-button','action-area'].includes(id))return [['class','button / ds-outline / ds-text','Solid·Outlined·Text 스타일'],['data-size','sm / md / lg','32 / 40 / 48px 높이'],['disabled','boolean','포커스·클릭 비활성화'],['aria-busy','true / false','로딩 상태 전달'],['aria-label','string','아이콘 단독 버튼의 이름']];
 if(['checkbox','radio','switch','framed-style','check-mark','chip','filter-button','segmented-control','category'].includes(id))return [['checked / aria-pressed','boolean','입력 또는 버튼의 선택 상태'],['name','string','라디오 그룹의 단일 선택'],['disabled','boolean','변경 불가능한 입력 상태'],['label / aria-label','string','선택 항목의 이름']];
 if(['text-field','text-area','search-field','select','autocomplete','date-picker','time-picker','slider'].includes(id))return [['id + label[for]','string','입력과 레이블 연결'],['value','string / number','현재 입력값'],['required / disabled','boolean','필수 여부와 비활성 상태'],['aria-invalid','true / false','검증 오류 상태'],['aria-describedby','element id','도움말·오류 설명 연결'],['min / max / maxlength','number / date','입력 범위와 길이 제한']];
 if(['popup','alert','bottom-sheet','focus-scope','portal','use-alert'].includes(id))return [['modal(title, html)','function','공통 dialog를 열고 트리거를 기억'],['dialog.showModal()','native API','top layer와 포커스 제약'],['data-close','attribute','공통 닫기 핸들러'],['dialog.close()','native API','닫기 및 포커스 복귀']];
 if(['popover','menu','popper','dismissable-layer'].includes(id))return [['popover','auto','Escape와 외부 클릭 닫기'],['popovertarget','element id','트리거와 레이어 연결'],['positionLayer(anchor, layer)','function','화면 경계에 맞춰 위치 계산'],['showPopover() / hidePopover()','native API','레이어 표시·닫기']];
 if(id==='tab')return [['role','tablist / tab / tabpanel','탭 위젯의 역할'],['aria-selected','true / false','현재 선택된 탭'],['aria-controls / aria-labelledby','element id','탭과 패널 연결'],['tabindex','0 / -1','선택 탭만 순차 포커스에 포함']];
 if(id==='add-opacity')return [['addOpacity(hex, alpha)','string, number → string','#RRGGBB 색상과 0–1 투명도'],['TypeError','exception','잘못된 색상 또는 범위 차단']];
 if(id==='region-config')return [['formatRegion(locale, date?)','function → object','지역별 날짜와 숫자 문자열'],['Intl.DateTimeFormat','native API','Asia/Seoul 시간대'],['Intl.NumberFormat','native API','자리 구분자 지역화']];
 if(id==='typography-style')return [['getTypographyStyle(name)','function → CSS string','19개 타입 이름 중 하나'],['TypeError','exception','정의되지 않은 스타일 차단']];
 if(id==='use-size')return [['ResizeObserver','native API','요소 크기 관찰'],['contentRect','width / height','콘텐츠 영역 크기'],['disconnect()','method','페이지 종료 시 관찰 해제']];
 if(['force-theme','use-theme-control'].includes(id))return [['data-ds-theme','light / dark','영역별 의미 토큰 전환'],['--semantic-*','CSS custom property','테마별 색상'],['--atomic-*','CSS custom property','고정 원시 팔레트']];
 if(['toast','use-toast','snackbar','use-snackbar'].includes(id))return [['toast(message)','function','공통 role=status에 메시지 출력'],['role','status','작업 결과 안내'],['data-undo','attribute (snackbar)','보관 상태를 되돌리는 액션']];
 if(['pagination','pagination-dots','page-counter','progress-tracker'].includes(id))return [['aria-current','page / step','현재 위치'],['disabled','boolean','이동 경계에서 비활성화'],['data-page-step / data-step','-1 / 1','앞·뒤 이동량']];
 if(id==='progress-indicator')return [['value','0–100','현재 진행률'],['max','100','완료 기준'],['label[for]','element id','진행률 레이블']];
 if(id==='accordion')return [['details[open]','boolean','현재 펼침 상태'],['summary','element','키보드로 조작하는 제목'],['content','HTML','펼쳐진 설명 영역']];
 if(id==='tooltip')return [['aria-describedby','tooltip id','버튼과 설명 연결'],['role','tooltip','보충 설명의 역할'],[':hover / :focus-within','CSS states','포인터·키보드 표시']];
 return [['class','코드 예제 참조','토큰 기반 스타일 적용'],['semantic HTML','의미에 맞는 요소','링크·제목·목록·상태의 의미 유지'],['CSS custom properties','아래 연결 토큰 참조','색상·간격·형태 조정']];
}
function relatedTokens(d){let prefixes=d.section==='utilities'?['--space-','--semantic-background','--type-body-1-']:d.group==='Actions'?['--control-','--focus-ring']:d.group==='Selection and input'?['--input-','--control-height','--focus-ring']:d.group==='Presentation'?['--dialog-','--tooltip-','--z-']:d.group==='Loading'?['--duration-','--semantic-fill']:d.group==='Contents'?['--card-','--avatar-','--badge-']:['--semantic-status-','--semantic-primary-','--space-'];return tokens.filter(t=>prefixes.some(p=>t.name.startsWith(p))).slice(0,16);}
function detail(d){const html=d.section==='components'?renderComponent(d.id):renderUtility(d.id),buttonLike=['button','icon-button','text-button'].includes(d.id);return `<div class="ds-doc-lead"><div class="ds-breadcrumb"><a href="${url(d.section)}">${sectionNames[d.section]}</a><span>/</span><span>${d.group}</span></div><h1>${d.name}</h1><p>${d.description}</p><a class="ds-reference" href="${d.source}" target="_blank" rel="noopener">Montage 원문 보기 ${icon('arrow-right-up-line')}</a></div><section class="ds-doc-section" id="example"><div class="ds-heading-row"><h2>Playground</h2><span class="ds-badge">Vanilla Web</span></div>${buttonLike?`<div class="ds-playground-controls"><label>Variant<select class="ds-input" data-play-variant><option value="solid">Solid</option><option value="outline">Outlined</option><option value="text">Text</option></select></label><label>Size<select class="ds-input" data-play-size><option value="sm">Small · 32</option><option value="md" selected>Medium · 40</option><option value="lg">Large · 48</option></select></label><label>State<select class="ds-input" data-play-state><option value="default">Default</option><option value="disabled">Disabled</option><option value="loading">Loading</option></select></label></div>`:''}<div class="ds-demo" id="component-preview">${html}</div><details class="ds-code-panel"><summary>코드 보기 · HTML</summary><div class="ds-code-toolbar"><span>프로젝트 CSS와 함께 사용</span><button class="ds-copy" data-copy-code aria-label="예제 코드 복사">${icon('file-copy-line')} 복사</button></div><pre><code id="example-code">${esc(html)}</code></pre></details></section>${variants(d)?`<section class="ds-doc-section" id="variants"><h2>Variants</h2><div class="ds-demo">${variants(d)}</div></section>`:""}<section class="ds-doc-section" id="anatomy"><h2>${d.section==='utilities'?'Implementation':'Anatomy'}</h2>${d.section==='utilities'?`<div class="ds-callout">${d.usage}</div>`:`<div class="ds-anatomy">${(anatomy[d.group]||['Container','Content']).map((n,i)=>`<div><b>${i+1}</b><span>${n}</span></div>`).join('')}</div>`}</section><section class="ds-doc-section" id="states"><h2>States & Behavior</h2>${buttonLike?`<div class="ds-demo ds-flex"><button class="button">Default</button><button class="button ds-state-hover">Hover</button><button class="button ds-state-pressed">Pressed</button><button class="button ds-state-focus">Focus</button><button class="button" disabled>Disabled</button><button class="button" aria-busy="true" disabled><span class="ds-spinner" aria-hidden="true"></span>Loading</button></div>`:`<div class="ds-behavior-grid"><div><strong>Default</strong></div><div><strong>Interaction</strong><p>${d.usage}</p></div><div><strong>Keyboard & Motion</strong><p>Tab으로 조작 요소를 이동합니다. 네이티브 입력의 키보드 동작을 유지하며 동작 감소 설정을 존중합니다.</p></div></div>`}</section><section class="ds-doc-section" id="api"><h2>API / Markup</h2><p>이 프로젝트에서 사용하는 HTML 속성과 Vanilla API입니다.</p><div class="ds-table-scroll"><table class="ds-table"><thead><tr><th>이름</th><th>형식 / 값</th><th>역할</th></tr></thead><tbody>${apiRows(d).map(row=>`<tr>${row.map((x,i)=>`<td>${i===0?`<code>${esc(x)}</code>`:esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table></div></section>${implementationCode(d)}<section class="ds-doc-section" id="usage"><h2>Usage & Accessibility</h2><div class="ds-guidelines"><div><strong>권장</strong><p>${d.usage}</p></div><div><strong>공통 기준</strong><p>아이콘 액션에는 이름을 제공합니다. 프로젝트 요청에 따라 버튼과 입력창 포커스 아웃라인은 숨기며 키보드 조작은 유지합니다. 비활성 상태·오류·진행 상황은 시각 효과와 텍스트를 함께 사용합니다.</p></div></div></section><section class="ds-doc-section" id="tokens"><h2>Connected tokens</h2>${tokenTable(relatedTokens(d))}<a class="ds-reference" href="${url('foundations','tokens')}">전체 토큰 탐색 ${icon('arrow-right-line')}</a></section>`;}
export function designSystem(){const {section,item}=current(),list=section==='foundations'?foundations:section==='components'?components:utilities,d=list.find(x=>x.id===item),content=item==='overview'?(section==='foundations'?renderFoundation('overview'):overview(section)):d?(section==='foundations'?renderFoundation(item):detail(d)):'<div class="ds-doc-lead"><h1>문서를 찾을 수 없습니다</h1><p>왼쪽 메뉴에서 항목을 선택해주세요.</p></div>',groups=[...new Set(list.map(x=>x.group))];return `<div id="ds-root" data-ds-theme="light"><div class="ds-docbar"><nav aria-label="디자인시스템 분류">${Object.entries(sectionNames).map(([id,n])=>`<a href="${url(id)}" ${section===id?'aria-current="page"':''}>${n}</a>`).join('')}</nav><div class="ds-docbar-tools"><label class="ds-search-box">${icon('search-line')}<input class="input-plain" type="search" id="ds-doc-search" placeholder="문서 검색" aria-label="디자인시스템 문서 검색" aria-controls="ds-search-results"></label><button class="ds-theme-toggle" aria-label="다크 프리뷰 켜기" aria-pressed="false">${icon('moon-line')}</button></div></div><div id="ds-search-results" hidden aria-label="문서 검색 결과"></div><div class="ds-doclayout"><aside class="ds-docsidebar"><span class="ds-eyebrow">${sectionNames[section]}</span><a class="ds-overview-link" href="${url(section)}" ${item==='overview'?'aria-current="page"':''}>Overview</a><button class="ds-mobile-index" aria-expanded="false" aria-controls="ds-side-navigation">문서 목차 펼치기</button><nav id="ds-side-navigation" aria-label="${sectionNames[section]} 문서 목록">${groups.map(g=>`<details open><summary>${g}</summary>${list.filter(x=>x.group===g&&x.id!=='overview').map(x=>`<a href="${url(section,x.id)}" ${item===x.id?'aria-current="page"':''}>${x.name}</a>`).join('')}</details>`).join('')}</nav><div class="ds-sidebar-note">KWSA Design System<br><span>v3.1 · ${tokens.length} tokens</span></div></aside><article class="ds-doccontent">${content}</article><aside class="ds-toc" aria-label="이 페이지 목차"><strong>On this page</strong><nav>${(item==='overview'||section==='foundations'?[]:[['example','Playground'],['anatomy',section==='utilities'?'Implementation':'Anatomy'],['states','States & Behavior'],['api','API / Markup'],['usage','Usage'],['tokens','Tokens']]).map(([id,n])=>`<a href="#${id}">${n}</a>`).join('')}</nav><a class="ds-reference" href="/assets/design-system/tokens.json" download>JSON 내려받기 ${icon('download-line')}</a></aside></div><div id="ds-announcement" class="sr-only" role="status"></div></div>`;}
export function bindDesignSystem(){if(document.body.dataset.page!=='design-system')return;const root=document.getElementById('ds-root');
 const menu=root.querySelector('.ds-mobile-index');menu.onclick=()=>{const expanded=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(expanded));menu.textContent=expanded?'문서 목차 접기':'문서 목차 펼치기';};
 const values=()=>{const styles=getComputedStyle(root);root.querySelectorAll('[data-token-value]').forEach(el=>el.textContent=styles.getPropertyValue(el.dataset.tokenValue).trim());};values();
 root.querySelector('.ds-theme-toggle').onclick=e=>{const dark=root.dataset.dsTheme!=='dark';root.dataset.dsTheme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.setAttribute('aria-label',dark?'라이트 프리뷰 켜기':'다크 프리뷰 켜기');e.currentTarget.innerHTML=icon(dark?'sun-line':'moon-line');values();};
 const copy=async text=>{try{await navigator.clipboard.writeText(text);toast('클립보드에 복사했습니다.');}catch{toast('복사 권한이 없습니다. 코드의 텍스트를 직접 선택해주세요.');}};root.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>copy(b.dataset.copy));root.querySelector('[data-copy-code]')?.addEventListener('click',()=>copy(root.querySelector('#example-code').textContent));
 const search=root.querySelector('#ds-doc-search'),results=root.querySelector('#ds-search-results');search.oninput=()=>{const q=search.value.trim().toLowerCase();results.hidden=!q;if(!q)return;const hits=allDocs.filter(d=>(d.name+' '+d.description+' '+d.group+' '+d.keywords).toLowerCase().includes(q));results.innerHTML=`<p role="status">${hits.length}개 문서</p><div>${hits.map(d=>`<a href="${url(d.section,d.id)}"><strong>${d.name}</strong><small>${sectionNames[d.section]} / ${d.group}</small></a>`).join('')||'<p>검색 결과가 없습니다. 다른 이름으로 검색해주세요.</p>'}</div>`;};search.onkeydown=e=>{if(e.key==='Escape'){search.value='';results.hidden=true;}};
 const filter=()=>{const q=root.querySelector('#token-search').value.toLowerCase(),g=root.querySelector('#token-group').value;let count=0;root.querySelectorAll('[data-token-row]').forEach(row=>{row.hidden=!(row.dataset.search.toLowerCase().includes(q)&&(!g||row.dataset.search.includes(g)));if(!row.hidden)count++;});root.querySelector('#token-count').textContent=count+'개 토큰';};root.querySelector('#token-search')?.addEventListener('input',filter);root.querySelector('#token-group')?.addEventListener('change',filter);
 const typeUpdate=()=>root.querySelectorAll('[data-type-preview]').forEach(p=>{p.textContent=root.querySelector('#type-preview-text').value||' ';p.style.fontWeight=root.querySelector('#type-preview-weight').value;});root.querySelector('#type-preview-text')?.addEventListener('input',typeUpdate);root.querySelector('#type-preview-weight')?.addEventListener('change',typeUpdate);
 root.querySelectorAll('[data-variant-toast]').forEach(b=>b.onclick=()=>toast('크기와 스타일 예시입니다.'));root.querySelectorAll('[data-variant-toggle]').forEach(b=>b.onclick=()=>b.setAttribute('aria-pressed',String(b.getAttribute('aria-pressed')!=='true')));
 const preview=root.querySelector('#component-preview');if(preview){bindExamples(preview);const original=preview.innerHTML;const update=()=>{const variant=root.querySelector('[data-play-variant]').value,size=root.querySelector('[data-play-size]').value,state=root.querySelector('[data-play-state]').value;preview.innerHTML=original;preview.querySelectorAll('button').forEach(b=>{b.classList.remove('ds-outline','ds-text');if(variant!=='solid')b.classList.add('ds-'+variant);b.dataset.size=size;b.disabled=state!=='default';b.setAttribute('aria-busy',String(state==='loading'));if(state==='loading')b.insertAdjacentHTML('afterbegin','<span class="ds-spinner" aria-hidden="true"></span>');});root.querySelector('#example-code').textContent=preview.innerHTML;bindExamples(preview);};root.querySelectorAll('[data-play-variant],[data-play-size],[data-play-state]').forEach(s=>s.onchange=update);}
 const toc=root.querySelector('.ds-toc nav');if(!toc.children.length){root.querySelectorAll('.ds-doccontent h2').forEach((h,i)=>{if(!h.id)h.id='doc-heading-'+i;const a=document.createElement('a');a.href='#'+h.id;a.textContent=h.textContent;toc.append(a);});}
 document.getElementById('modal').addEventListener('close',e=>{e.target.classList.remove('ds-bottom-sheet');e.target.querySelectorAll('video').forEach(v=>v.pause());delete e.target.dataset.dsTheme;});
}

import {tokenGroups} from './tokens.js';
import {esc,icon} from '../renderers.js';
import {typeNames,getTypographyStyle} from './utilities.js';
export const tokens=tokenGroups.flatMap(g=>g.tokens.map(t=>({...t,group:g.id,groupTitle:g.title})));
export const tokenTable=(list)=>`<div class="ds-table-scroll"><table class="ds-table ds-token-table"><thead><tr><th scope="col">Token</th><th scope="col">정의</th><th scope="col">현재 값</th><th scope="col">복사</th></tr></thead><tbody>${list.map(t=>`<tr data-token-row data-search="${esc(t.name+' '+t.value+' '+t.groupTitle)}"><td><code>${esc(t.name)}</code><small>${esc(t.groupTitle)}</small></td><td><code>${esc(t.value)}</code>${t.dark?`<small>Dark: ${esc(t.dark)}</small>`:''}</td><td><code data-token-value="${t.name}"></code></td><td><button class="ds-copy" data-copy="var(${t.name})" aria-label="${t.name} 복사">${icon('file-copy-line')}</button></td></tr>`).join('')}</tbody></table></div>`;
const swatch=t=>`<button class="ds-color-swatch" data-copy="var(${t.name})" aria-label="${t.name} 복사"><span style="background:var(${t.name})"></span><strong>${t.name.replace('--atomic-','').replace('--semantic-','')}</strong><code data-token-value="${t.name}"></code></button>`;
export function renderFoundation(id){switch(id){
 case 'overview':return `<div class="ds-doc-lead"><h1>Foundations</h1><p>일관된 화면의 시작은 명확한 토큰입니다. 색상부터 상호작용까지, 프로젝트 전체가 공유하는 기본 규칙을 정의합니다.</p></div><div class="ds-metrics"><div><b>${tokens.length}</b><span>Design tokens</span></div><div><b>19</b><span>Type styles</span></div><div><b>2</b><span>Preview themes</span></div></div><section class="ds-doc-section"><h2>토큰의 세 단계</h2><div class="ds-token-chain"><div><small>01 · Primitive</small><code>--brand-primary</code><b>#176B45</b></div><span>→</span><div><small>02 · Semantic</small><code>--semantic-primary-normal</code><b>주요 행동 색상</b></div><span>→</span><div><small>03 · Component</small><code>--control-bg</code><b>버튼 배경</b></div></div><p>새 컴포넌트는 의미별 토큰을 참조합니다. Layout은 프로젝트 구조, Local은 화면별 예외를 담당합니다. 전환이 끝난 --color-* 별칭은 제거했습니다.</p></section><section class="ds-doc-section"><h2>문서 사용법</h2><div class="ds-overview-cards">${[['colors','Colors','공식 원시 팔레트와 의미별 색상'],['typography','Typography','크기·행간·자간·굵기의 조합'],['elevation','Elevation','표면의 깊이와 레이어 순서'],['tokens','Token explorer','토큰 검색과 CSS 변수 복사']].map(([id,n,d])=>`<a href="?section=foundations&item=${id}"><span class="ds-overview-symbol">${icon(id==='colors'?'palette-line':id==='typography'?'font-size':id==='elevation'?'stack-line':'code-s-slash-line')}</span><h3>${n}</h3><p>${d}</p></a>`).join('')}</div></section><div class="ds-callout"><strong>Montage 구조를 따른 KWSA 구현</strong><p>Montage의 문서 분류·타입 스케일·상태 모델을 참고했습니다. 색상과 컴포넌트 코드는 이 프로젝트의 그린 브랜드 및 PHP/Vanilla 환경에 맞게 구현되어 있으며 공식 Montage 패키지와 API가 동일하지 않습니다.</p></div>`;
 case 'colors':return `<div class="ds-doc-lead"><h1>Colors</h1><p>원시 팔레트는 색의 단계, 의미별 토큰은 사용 목적을 정의합니다. 색상 칩을 누르면 CSS 변수가 복사됩니다.</p></div><section class="ds-doc-section" id="semantic"><h2>Semantic colors</h2><p>라이트/다크 프리뷰를 전환하면 의미별 색상 매핑이 바뀝니다. 브랜드 색상은 KWSA 그린을 사용합니다.</p><div class="ds-color-grid">${tokens.filter(t=>t.group==='semantic').map(swatch).join('')}</div></section><section class="ds-doc-section" id="atomic"><h2>Atomic colors</h2><p>Montage 공식 팔레트입니다. 각 색상에 공개된 단계만 표시합니다.</p>${[...new Set(tokens.filter(t=>t.group==='atomic').map(t=>t.name.split('-')[3]))].map(c=>`<h3 class="ds-palette-title">${c}</h3><div class="ds-palette">${tokens.filter(t=>t.name.startsWith('--atomic-'+c+'-')).map(swatch).join('')}</div>`).join('')}</section><section class="ds-doc-section"><h2>사용 원칙</h2><div class="ds-guidelines"><div><strong>권장</strong><p>본문은 Label Normal, 보조 설명은 Label Alternative를 사용합니다. 상태는 텍스트와 함께 표시합니다.</p></div><div><strong>주의</strong><p>Assistive·Disable 색상을 중요한 본문에 사용하지 않습니다. 투명 라인은 중첩하면 진해질 수 있습니다.</p></div></div></section>`;
 case 'typography':return `<div class="ds-doc-lead"><h1>Typography</h1><p>Pretendard Variable. 최소 13px의 프로젝트 타이포그래피입니다. Montage 기준에서 크기는 2px, 행간은 4px 확대하고 자간은 새 크기에 맞춰 조정했습니다. Caption 1은 14px / 20px / 0.0145em, Caption 2는 13px / 18px / 0.0194em입니다. 공식 원본과 구분되는 프로젝트 규격입니다. 한글 사이트는 기존 Pretendard Variable을 유지하며 공식 문서는 Pretendard JP를 안내합니다.</p></div><div class="ds-type-controls"><label>미리보기 문구<input class="ds-input" id="type-preview-text" value="일상에 스포츠를 더하다" maxlength="80"></label><label>굵기<select class="ds-input" id="type-preview-weight"><option value="400">Regular 400</option><option value="500">Medium 500</option><option value="600" selected>Semibold 600</option><option value="700">Bold 700</option></select></label></div><div class="ds-type-specimens">${typeNames.map(n=>`<article><div><strong>${n}</strong><code>${tokens.find(t=>t.name===`--type-${n}-size`).value} / ${tokens.find(t=>t.name===`--type-${n}-line`).value}</code><small>${tokens.find(t=>t.name===`--type-${n}-tracking`).value}</small></div><p data-type-preview style="${getTypographyStyle(n)};font-weight:var(--font-weight-semibold)">일상에 스포츠를 더하다</p></article>`).join('')}</div><section class="ds-doc-section"><h2>의미와 모양을 분리</h2><p>타입 클래스는 시각 스타일을 정합니다. 제목의 h1~h6 계층은 콘텐츠 구조에 맞춰 선택합니다. 한국어 줄바꿈은 기존 사이트의 keep-all 정책을 유지합니다.</p><pre><code>${esc('<h2 class="type-title-3">교육 프로그램</h2>\n<p class="type-body-1">나에게 맞는 프로그램을 찾아보세요.</p>')}</code></pre></section>`;
 case 'elevation':return `<div class="ds-doc-lead"><h1>Elevation</h1><p>Normal은 아래 방향의 깊이, Spread는 사방으로 퍼지는 깊이를 표현합니다.</p></div>${['normal','spread'].map(kind=>`<section class="ds-doc-section"><h2>${kind==='normal'?'Normal':'Spread'}</h2><div class="ds-elevation-grid">${tokens.filter(t=>t.name.startsWith('--semantic-elevation-shadow-'+kind+'-')).map(t=>`<div style="box-shadow:var(${t.name})"><strong>${t.name.split('-').at(-1)}</strong><code>${t.name}</code></div>`).join('')}</div></section>`).join('')}<section class="ds-doc-section"><h2>Layer order</h2>${tokenTable(tokens.filter(t=>t.name.startsWith('--z-')))}<p>네이티브 dialog·popover는 브라우저 top layer에 표시됩니다. z-index는 일반 문서 레이어 간의 순서를 정합니다.</p></section>`;
 case 'grid':return `<div class="ds-doc-lead"><h1>Grid</h1><p>컨테이너와 간격을 공유하고 화면 폭에 맞춰 열 수를 조절합니다.</p></div><section class="ds-doc-section"><h2>Responsive columns</h2><div class="ds-grid-ruler">${Array.from({length:12},(_,i)=>`<span>${i+1}</span>`).join('')}</div><div class="ds-demo-grid">${['카드 1','카드 2','카드 3'].map(t=>`<div class="ds-box">${t}</div>`).join('')}</div><p>문서 예제: Desktop 3열 → Tablet 2열 → Mobile 1열.</p></section>${tokenTable(tokens.filter(t=>t.group==='layout'))}<div class="ds-callout">CSS 변수는 미디어쿼리 조건에서 직접 사용할 수 없습니다. 브레이크포인트는 문서화된 값에 맞춰 CSS 미디어쿼리에 사용합니다.</div>`;
 case 'icons':return `<div class="ds-doc-lead"><h1>Icons</h1><p>프로젝트에 포함된 Remix Icon 4.6.0을 사용합니다. Montage 아이콘 패키지를 설치한 것은 아닙니다.</p></div><section class="ds-doc-section"><h2>Size</h2><div class="ds-icon-sizes">${['xs','sm','md','lg','xl'].map(n=>`<div><i class="ri-trophy-line" aria-hidden="true" style="font-size:var(--icon-${n})"></i><code>${n} · ${tokens.find(t=>t.name==='--icon-'+n).value}</code></div>`).join('')}</div></section><section class="ds-doc-section"><h2>Project icon set</h2><div class="ds-icon-grid">${['home-5-line','search-line','user-line','team-line','trophy-line','calendar-line','map-pin-line','book-open-line','medal-line','shield-check-line','heart-line','notification-3-line','check-line','close-line','arrow-right-line','menu-line','information-line','error-warning-line','file-copy-line','download-line','settings-3-line','filter-3-line','play-line','pause-line'].map(n=>`<button data-copy="ri-${n}" aria-label="ri-${n} 클래스 복사">${icon(n)}<code>${n}</code></button>`).join('')}</div></section><div class="ds-callout">장식 아이콘은 aria-hidden="true"를 사용합니다. 아이콘 단독 버튼에는 aria-label을 주고 44px 이상의 조작 영역을 확보합니다.</div>`;
 case 'spacing':return `<div class="ds-doc-lead"><h1>Spacing & Radius</h1><p>간격과 형태를 토큰으로 통일합니다. 필요 이상으로 새로운 수치를 추가하지 않습니다.</p></div><section class="ds-doc-section"><h2>Spacing scale</h2><div class="ds-space-scale">${tokens.filter(t=>t.name.startsWith('--space-')).map(t=>`<div><code>${t.name}</code><span style="width:var(${t.name})"></span><b>${t.value}</b></div>`).join('')}</div></section><section class="ds-doc-section"><h2>Radius</h2><div class="ds-radius-grid">${tokens.filter(t=>t.name.startsWith('--radius-')).map(t=>`<div style="border-radius:var(${t.name})"><code>${t.value}</code></div>`).join('')}</div></section>${tokenTable(tokens.filter(t=>t.name.startsWith('--border-')||t.name.startsWith('--control-height')))}`;
 case 'motion':return `<div class="ds-doc-lead"><h1>Motion & Interaction</h1><p>모션은 상태 변화를 설명하는 데 사용합니다. 사용자의 동작 감소 설정을 우선합니다.</p></div><div class="ds-demo">${['fast','normal','slow'].map(n=>`<button class="ds-motion-demo" style="--example-duration:var(--duration-${n})">${n}<span></span></button>`).join('')}<p class="ds-help">포인터를 올리거나 키보드 포커스를 이동해 비교하세요.</p></div>${tokenTable(tokens.filter(t=>t.group==='motion'))}<div class="ds-callout">prefers-reduced-motion 환경에서는 애니메이션과 전환 시간을 최소화합니다. 프로젝트 요청에 따라 버튼·입력창의 포커스 아웃라인은 표시하지 않습니다. 키보드 조작은 유지됩니다.</div>`;
 case 'tokens':return `<div class="ds-doc-lead"><h1>Token explorer</h1><p>JSON 원본에서 생성된 ${tokens.length}개 토큰입니다. 정의와 현재 테마의 계산값을 비교하고 CSS 변수를 복사할 수 있습니다.</p></div><div class="ds-token-filter"><label>토큰 검색<input class="ds-input" id="token-search" type="search" placeholder="primary, radius, control…"></label><label>분류<select class="ds-input" id="token-group"><option value="">전체</option>${tokenGroups.map(g=>`<option value="${g.title}">${g.title}</option>`).join('')}</select></label></div><p id="token-count" role="status">${tokens.length}개 토큰</p>${tokenTable(tokens)}<div class="ds-callout">원본: assets/design-system/tokens.json → python scripts/build-design-tokens.py → tokens.css / tokens.js</div>`;
 default:return '<h1>문서를 찾을 수 없습니다</h1>';
}}
